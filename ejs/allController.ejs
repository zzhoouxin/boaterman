<% fileList.forEach((item)=>{ %>
    //<%= item.describeName%>
    import <%= item.name%> from './<%=item.name%>';
<% })%>    

export default {
    <% fileList.forEach((item)=>{ %>
       <%= item.name%>,
    <% })%> 
    
}